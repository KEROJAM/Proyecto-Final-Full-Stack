{
	"name": "todo-backend",
	"version": "1.0.0",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "nodemon server.js",
		"setup": "echo 'Creando base de datos...' && mysql -u root -p < ../mysql.sql"
	},
	"dependencies": {
		"cors": "^2.8.6",
		"dotenv": "^17.2.3",
		"express": "^4.18.2",
		"mysql2": "^3.6.0"
	},
	"devDependencies": {
		"nodemon": "^3.1.11"
	}
}
